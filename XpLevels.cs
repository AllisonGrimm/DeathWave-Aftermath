using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class XpLevels : MonoBehaviour
{
    public static void UpdateXp(Stats target)
    {
        //case statement that contains all the xp levels for each level up for instance if level = 1 then 500 or something and sets that in target
        //updates total xp needed xp doesn't reset to 0 when level up
    }
}
